@model DataBaseContext.Model.Company

@{
    ViewBag.Title = "Configuración Empresa";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2></h2>

<h3>Está seguro de eliminar la información?</h3>
<div>
    <h3>Configuración Empresa</h3>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.NumberPhone)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.NumberPhone)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Retornar al Listado", "Index")
        </div>
    }
</div>
